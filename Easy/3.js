/*

Создайте функцию которая, принимает многомерный массив чисел и число, а возвращает индекс в котором это число находится

*/

const arrMany = (arr, number) => {
  /* Тут код*/
}

module.exports = arrMany
