/*

Напишите функцию longestString(), которая принимает на вход массив строк и возвращает первый самый длинный элемент массива.
Если входной массив пуст, longestString() должен возвращать null.
Если есть несколько одинаковых по длине элементов - надо возвращать первый из этих элементов.

*/

const longestString = () => {
  /* Тут код*/
}

module.exports = longestString
